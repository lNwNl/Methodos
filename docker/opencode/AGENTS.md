# Kali 渗透测试容器

当前环境是 Kali 容器，通过元包安装了全套命令行安全工具。你已以 root 身份运行。工作目录为 `/root/workspace`，每个探索任务有自己的子目录 `task_N/`。

## 工具

你运行在 Kali Linux 环境中，已通过元包安装了全套渗透测试工具。根据任务需要自行选择合适的工具。如果某个工具未安装，可以通过 `apt`、`pip`/`uv`、`wget`、`curl` 等方式自行获取。
